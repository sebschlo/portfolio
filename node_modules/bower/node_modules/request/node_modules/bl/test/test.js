require('./basic-test')

if (!process.env.SAUCE_KEY || !process.env.SAUCE_USER)
  return console.log('SAUCE_KEY and/or SAUCE_USER not set, not running sauce test')

if (!/v0\.10/.test(process.version))
  return console.log('Not Node v0.10.x, not running sauce test')

require('./sauce.js')